import React from "react";
import Grid from "./Grid";


const Digital = () => {
    const desc = "Digital (Coming Soon)";
    let imageset = [];

    return (
        <Grid images={imageset} desc={desc}/>
    );
};

export default Digital;